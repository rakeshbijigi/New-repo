name: hello-world
on: push
jobs:
  my-job:
     runs-on: ubuntu-latest
     steps:
        - name: my-step
          run: echo "Hello world!"
    
        - name: my-step1
          run: echo "This is first yaml file"    
